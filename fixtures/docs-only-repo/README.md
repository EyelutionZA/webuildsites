# Docs Only Repo

This repo contains documentation only. It should not be classified as a deployable app.

The detector should ignore markdown content and only treat source code and package dependencies as runtime evidence.