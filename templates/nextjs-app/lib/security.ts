export const securityNotes = [
  'Configure authentication provider and session strategy before production.',
  'Define database backups and restore process.',
  'Verify payment webhooks and secrets before accepting live payments.',
  'Add role/permission checks before exposing private data.'
] as const;
